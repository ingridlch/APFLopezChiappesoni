import { TitlecolumnDirective } from './titlecolumn.directive';

describe('TitlecolumnDirective', () => {
  it('should create an instance', () => {
    const directive = new TitlecolumnDirective();
    expect(directive).toBeTruthy();
  });
});
